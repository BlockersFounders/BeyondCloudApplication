<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="founders.blockers.beyondcloudapp">


    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_cloudd"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Record" />
        <activity android:name=".MeFormActivity" />
        <activity android:name=".MyLoveFormActivity" />
        <activity android:name=".ui.addBlock.ChooseAddType_3Fragment" />
        <activity android:name=".ui.addBlock.ChooseAddType_2Fragment" />
        <activity android:name=".ui.addBlock.ChooseAddType_1Fragment" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>